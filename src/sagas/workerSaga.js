import { call, put, takeEvery, takeLatest } from 'redux-saga/effects'
import api from '../api/index'

export default function* getAllClinicsWorker(action) {
      console.log('getAllClinicsWorker')
 }